spring:
  datasource:
    url: jdbc:h2:mem:nexora;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  kafka:
    listener:
      auto-startup: false
nexora:
  events:
    enabled: false
